import Container from '@/common/Container/Container';
import styles from './BookFreeWorkout.module.sass';

const VerificationAndBook = () => {
  return <Container className={styles.wrapper}>Verification and Book</Container>;
};

export default VerificationAndBook;
